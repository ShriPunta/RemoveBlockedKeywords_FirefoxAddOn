{
  "devDependencies": {
    "@types/firefox-webext-browser": "^120.0.3",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.5",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  },
  "scripts": {
    "build": "npx webpack --config webpack.config.js && cp src/popup.html src/popup.js dist/",
    "package": "npm run build && VERSION=$(node -p \"require('./manifest.json').version\") && zip -r \"releases/reddit-filter-addon-v$VERSION.zip\" manifest.json dist/ icons/ -x '*.DS_Store' '**/node_modules/*' '**/.git/*'"
  }
}
